{
  "name": "belkanstrelka",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "help": "next export -help",
    "deploy": "rm -rf node_modules/.cache && next build && next export -o docs && touch docs/.nojekyll && cp CNAME docs/CNAME && git add docs/ && git commit -m \"Deploy belkanstrelka\" && git push origin master"
  },
  "dependencies": {
    "next": "latest",
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
